tcsplus is a http server, that will echo to the terminal any json payload it receives.

nagoshi.py is an http server, which will activate two predetermine command in the local host, upon receiving two predetermine URLs.
